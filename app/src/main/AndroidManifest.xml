<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pablovilas.reversi">

    <application android:icon="@mipmap/icon_round" android:label="@string/app_name" android:allowBackup="true" android:theme="@style/AppTheme">
        <activity android:name=".activities.Menu_Principal" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".old.Configuration_old" android:label="Configuración"/>
        <activity android:name=".activities.Ayuda" android:label="Ayuda" />
        <!--<activity android:name=".activities.juego.Juego"/>-->
        <activity android:name=".activities.Resultados" android:label="Resultados"/>
        <activity android:name=".activities.configuration.Configuration" />
        <activity android:name=".activities.BaseActivity" />
        <activity android:name=".activities.listado_partidas.AccessBDActivity" />
        <activity android:name=".activities.listado_partidas.DetailRegActivity" />
        <activity android:name=".activities.juego.JuegoActivity" />
    </application>
</manifest>
